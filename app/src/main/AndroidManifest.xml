<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.tc3p0.prayertracker" >

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".PrayerActivity"
            android:label="@string/title_activity_prayer" >
        </activity>
        <activity
            android:name=".PrayerListActivity"
            android:label="@string/title_prayer_list"
            android:parentActivityName=".MainActivity" >
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.example.tc3p0.prayertracker.MainActivity" />
        </activity>
        <activity
            android:name=".PrayerDetailActivity"
            android:label="@string/title_prayer_detail"
            android:parentActivityName=".PrayerListActivity" >
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.example.tc3p0.prayertracker.PrayerListActivity" />
        </activity>
    </application>

</manifest>
